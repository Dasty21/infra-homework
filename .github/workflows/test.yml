name: test
on: push

jobs:
  dependencies:
    runs-on: ubuntu-latest
    steps:
      - name: dependencies
        run: yarn i

  linters:
    runs-on: ubuntu-latest
    needs: dependencies
    steps:
      - name: linters
        run: yarn linters

  build:
    runs-on: ubuntu-latest
    needs: dependencies
    steps:
      - name: build
        run: yarn build

  deploy:
    runs-on: ubuntu-latest
    needs: [dependencies, build]
    steps:
      - name: deploy
        run: yarn deploy
